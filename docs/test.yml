spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/vwholesale?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: vwholesale
    password: vwholesale
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: 127.0.0.1
      port: 6379
      database: 0
  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: vwholesale
    password: vwholesale
    publisher-confirm-type: correlated
    template:
      mandatory: true
    listener:
      simple:
        acknowledge-mode: auto
        retry:
          enabled: true
          max-attempts: 3
          initial-interval: 2s
          multiplier: 2
          max-interval: 30s
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

app:
  mq:
    enabled: true
  wechat:
    app-id: wxd056da1506f00ca1
    app-secret: 5d2d549aa267d69e701b3225aaa1b7c8
  upload:
    dir: /opt/vwholesale/uploads

logging:
  level:
    com.vwholesale: info